query GET_ACTIVITY_LIST($count: Int!, $authToken: String!) {
  activityList(count: $count, authToken: $authToken) {
    id
    name
    type
    subType
    startDate
    distance
    averageSpeed
  }
}
