query GET_ALL_RUN_TOTAL_STATS_QUERY($id: ID!, $type: String!, $interval: String!) {
  athleteStats(id: $id, type: $type, interval: $interval) {
    allRunTotal {
      count
      distance
      movingTime
      elapsedTime
      elevationGain
    }
  }
}
