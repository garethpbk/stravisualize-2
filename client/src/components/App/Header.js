import React from 'react';

// import styled components
import { HeaderWrapper, HeaderContainer } from './styled/HeaderStyled';

const Header = () => (
  <HeaderWrapper>
    <HeaderContainer>
      <h1>Stravisualize</h1>
    </HeaderContainer>
  </HeaderWrapper>
);

export default Header;
