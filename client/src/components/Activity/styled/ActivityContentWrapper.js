import styled from 'styled-components/macro';

const ActivityContentWrapper = styled.div`
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-column-gap: 15px;

  padding: 25px 0 50px 0;
`;

export default ActivityContentWrapper;
